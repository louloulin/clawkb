# UI Baseline

This directory keeps the latest product baseline screenshots for the current ClawKB workbench rewrite.

## Stable Screenshots

- `workbench-home.png`
- `spaces-registry.png`
- `document-workspace.png`

## Notes

- These screenshots replace the earlier top-level exploratory PNG files that mixed multiple product directions.
- Historical screenshots are archived under `docs/ui-baseline/archive/`.
- The baseline should be refreshed whenever a major phase changes the main product flow.
