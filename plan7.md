# ClawKB 闭环重构与产品收敛计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务执行本计划。执行步骤统一使用 checkbox（`- [ ]`）跟踪。

**Goal:** 基于当前仓库的真实代码、测试、UI 壳层、运行时约束与既有计划，重新定义 ClawKB 的产品定位、核心功能闭环与执行顺序，把当前“功能堆叠但主线混乱”的状态，收敛成“单用户、本地优先、围绕个人资料持续工作”的可执行改造路线图。

**Architecture:** 保留现有 Rust `clawkb-core` + `src-tauri` + React/Tauri 前端三层架构，不推倒重来；重点不是再加新功能，而是收敛产品主线、重组导航与状态模型、重建围绕 `KB → 资料 → 阅读 → 笔记 → 回流` 的闭环。

**Tech Stack:** Rust workspace (`clawkb-core`, `clawkb-cli`, `src-tauri`) + memvid-core + Tauri 2 + React 19 + TypeScript + Zustand + TipTap + shadcn/ui + Vitest + Cargo test

---

## 0. 本计划解决什么问题

当前 ClawKB 的主要问题不是“能力不够”，而是：

1. **定位表达与交互现实脱节**：产品口径说自己是本地优先个人知识库工作台，但 UI 呈现出来更像一个拼装的多功能实验台。
2. **主链路没有闭环**：用户从“打开知识库”到“完成一次有价值的知识工作”之间，路径过长、跳转过多、心智不稳定。
3. **高级能力抢走了首页与导航注意力**：图谱、报告、播客、时间线、标签、实体等能力都在抢主入口，结果核心工作反而不清楚。
4. **状态模型与页面职责碎裂**：页面、子视图、文档标签、空间切换、导入偏好、草稿状态分散在多套 store 里，容易让 UI 看起来“到处都能做事，但不知道哪里才是正解”。
5. **执行过程长期偏“叠功能”，没有持续删减和收口**：很多能力是真实现，但没有在产品层明确“什么是一等能力，什么是二等能力，什么应该隐藏到上下文里”。

本计划的目标，是先把 **产品闭环** 和 **功能等级** 梳理清楚，再安排后续代码改造。

---

## 1. 核心定位：ClawKB 到底应该是什么

### 1.1 一句话定位

**ClawKB 是一个面向单用户的、本地优先的个人知识工作台，用来围绕自己拥有的资料进行导入、检索、阅读、提问、记录与持续沉淀。**

### 1.2 它不应该是什么

ClawKB 当前不应该继续被做成：

- 一个云协作平台
- 一个团队文档中心
- 一个“工具大全”式的知识实验台
- 一个到处都有入口、但没有主轴的 AI 演示产品
- 一个先讲图谱/播客/报告，再让用户自己找笔记入口的系统

### 1.3 它真正服务的用户是谁

目标用户不是团队管理员，也不是知识管理极客，而是：

- 想把资料放在自己机器上的个人用户
- 想对自己导入的文档做搜索、阅读、提问、摘录和整理的知识工作者
- 想要“本地、稳定、简单、能持续积累”的人

### 1.4 核心产品承诺

ClawKB 应该明确向用户承诺四件事：

1. **你的资料默认只在本地。**
2. **你可以快速把资料放进来，并马上开始搜索和提问。**
3. **你可以在阅读中直接沉淀为自己的笔记，而不是把工作打散到很多页面。**
4. **高级能力是辅助层，不应该遮住主工作流。**

---

## 2. 核心功能闭环：产品真正应该围绕哪些闭环运转

当前系统最缺的不是功能点，而是闭环。未来所有页面、导航、命令、状态、测试，都应该围绕以下闭环重构。

### 2.1 闭环 A：知识库初始化闭环（第一次上手）

**目标：让新用户在 2-3 分钟内完成第一次有效工作。**

闭环定义：

1. 打开桌面应用
2. 打开已有 KB 或创建新 KB
3. 导入第一批资料（文件 / 文件夹 / URL / 截图）
4. 导入完成后立即进入搜索或问答
5. 得到第一条有效结果
6. 从结果继续阅读或保存为笔记

这个闭环是整个产品的“点火流程”。如果这个流程不顺，后面所有高级功能都没有意义。

### 2.2 闭环 B：资料检索与问答闭环（日常入口）

**目标：让用户把 ClawKB 当成自己资料的第一检索入口。**

闭环定义：

1. 用户输入问题或关键词
2. 系统在当前 KB 中检索资料
3. 返回可点击、可信、有来源的结果
4. 用户打开来源片段或文档
5. 用户继续追问 / 切换阅读 / 保存摘录

这条闭环里，AI 不是主角，**“基于自己资料的可信检索与延续工作”** 才是主角。

### 2.3 闭环 C：阅读到笔记的沉淀闭环（核心价值闭环）

**目标：让资料消费真正变成个人知识积累。**

闭环定义：

1. 用户打开某篇资料或搜索结果
2. 用户阅读、摘录、划线、选区提问
3. 用户把有价值内容转成笔记 / 草稿 /结构化记录
4. 笔记保存回当前 KB
5. 后续搜索、问答、时间线、标签再次能命中这条笔记

这是 ClawKB 最该成为“一等主线”的流程。没有它，系统只是“能搜的资料仓库”；有了它，系统才是“知识工作台”。

### 2.4 闭环 D：持续积累与回访闭环（留存闭环）

**目标：让用户不是只导入一次，而是长期回来继续工作。**

闭环定义：

1. 用户再次打开 KB
2. 系统恢复上次工作上下文（当前 KB、草稿、历史问答、阅读位置等）
3. 用户从最近资料 / 最近笔记 / 最近问答继续
4. 新一轮搜索、阅读、整理、保存继续发生

当前系统已经有部分本地恢复能力，但它们还是“技术上存在”，还没有变成“用户能感知的稳定体验”。

### 2.5 闭环 E：高级能力回流闭环（工具层闭环）

**目标：让高级能力服务主线，而不是自成入口。**

高级能力包括：

- OCR
- 时间线
- 标签
- 实体
- 图谱
- 报告
- 播客脚本
- WebDAV / Obsidian 导入

正确闭环应该是：

1. 用户先处在资料 / 阅读 / 笔记的主流程中
2. 某个上下文触发高级能力
3. 高级能力产出结果
4. 结果回到笔记、资料、问答或回访链路中

而不是：高级能力一开始就跟主功能一起抢首页和导航。

---

## 3. 核心功能分层：哪些是一等能力，哪些必须降级

### 3.1 一等能力（必须直接可见、可达、可测）

这些能力定义产品是否成立：

1. **打开 / 创建本地知识库**
2. **导入资料**
3. **搜索资料**
4. **基于资料提问**
5. **打开来源继续阅读**
6. **把阅读结果沉淀为笔记**
7. **恢复上次工作上下文**

### 3.2 二等能力（保留，但不应占主导航）

这些能力很有价值，但应该围绕主链路出现：

- 时间线
- 标签管理
- 实体浏览
- 图谱
- 选区 AI
- 导出
- Obsidian 导入
- WebDAV 同步

### 3.3 三等能力（仅在主线稳定后再加强）

- 报告生成
- 播客脚本生成
- 更复杂的多 KB 协作式视图
- 自动化 / 插件化预埋
- 更重的图可视化工作台

当前产品最大的问题之一，就是 **二等能力和三等能力过早抬到了主界面**。

---

## 4. 当前执行过程的问题：不是没做事，而是做事的方式出了问题

这一部分分析“为什么系统会走到今天这种功能很多但主线混乱的状态”。

### 4.1 问题一：先叠功能，再找主线

现在的实现过程明显是“能力持续增加”：

- 搜索
- 导入
- 标签
- 时间线
- 实体
- 图谱
- 报告
- 播客
- OCR
- WebDAV
- Obsidian
- 多 KB

这些都不是假能力，很多后端命令和前端页面都是真实现。但因为 **没有先定义“一次知识工作最短要走哪条路径”**，所以每增加一个功能都会继续稀释主线。

### 4.2 问题二：页面壳层先行，领域对象没有收口

当前很多页面已经存在，但系统中心对象仍然不稳定。

现在更像围绕这些对象运转：

- KB
- SearchHit
- ImportResult
- TimelineEntry
- EntityInfo
- FolderInfo

而不是围绕“持续工作对象”运转：

- 当前知识库
- 当前资料
- 当前阅读上下文
- 当前笔记
- 当前工作会话

结果是功能都能做一点，但用户不知道“我现在手里拿着的核心对象到底是什么”。

### 4.3 问题三：导航设计表达的是功能清单，不是用户任务

当前结构里存在多个 shell、多个 legacy page id、多层视图切换与内部 tabs。技术上可组织，产品上却不清晰。

表现为：

- 首页像问答页，但又承担导入和资料跳转
- Spaces 像知识库选择器，但又承载预览搜索与问答
- Documents 像笔记页，但实质上是 reader/draft/notes 混合工作台
- Explore 像工具总汇页，把不知该放哪的能力都装进去
- Settings 又背着 KB 打开/创建、AI 配置、同步、导出等关键能力

这不是“信息丰富”，而是**任务边界混乱**。

### 4.4 问题四：高级能力没有上下文化

高级能力本来应该是“在用户已经进入某份资料或某条笔记时，再被上下文触发”。

但当前很多能力是独立页面：

- graph
- report
- podcast
- entities
- timeline

这会导致用户被迫先理解工具本身，再理解怎么把结果带回工作流。

正确顺序应该反过来：**先工作，再在工作里调工具。**

### 4.5 问题五：执行上偏“加法”，缺少“删法”

过去的执行看起来更重视：

- 新增命令
- 新增页面
- 新增 store
- 新增测试

但较少做这些动作：

- 删除旧入口
- 合并重复页面
- 下沉高级功能
- 重新定义一等能力
- 把非核心体验从主路径挪走

这让产品越来越“能干”，但越来越“不清楚”。

### 4.6 问题六：验证过程偏技术验证，缺少任务闭环验证

现在的测试主要证明：

- 页面能渲染
- 文案符合预期
- 某些 store 行为正常
- 命令桥可调用

但还缺少面向真实任务的验收：

- 新用户能否 2 分钟内完成首次导入与首次问答
- 用户能否从一次问答顺滑进入来源阅读并保存笔记
- 用户第二次打开应用能否直接接续上次工作
- 用户是否能明确理解“这不是浏览器产品，而是桌面本地产品”

也就是说，当前验证更多在测“零件工作”，较少在测“闭环成立”。

---

## 5. 改造原则：后续所有设计与实现都要遵守的规则

### 5.1 原则一：主链路优先于能力完整度

如果一个功能会破坏“打开 KB → 导入 / 搜索 / 阅读 / 笔记”的主链路清晰度，就必须降级、折叠或延后。

### 5.2 原则二：先做减法，再做加法

优先动作不是“再加一个功能页”，而是：

- 合并入口
- 删除旧路径
- 降级高级功能
- 把设置页从业务页中剥离

### 5.3 原则三：页面按任务组织，不按功能清单组织

页面应该回答的是：

- 我现在在做什么？
- 下一步应该去哪？
- 当前对象是什么？

而不是：

- 系统还支持哪些酷炫模块？

### 5.4 原则四：AI 是能力层，不是导航层

AI 应该嵌在以下场景中：

- 搜索后的问答
- 阅读时解释 / 摘要 / 翻译 / 提问
- 写笔记时润色 / 整理 / 生成初稿

而不是单独把系统做成一个“AI 工具台”。

### 5.5 原则五：高级工具必须回流主对象

无论是图谱、时间线、报告还是播客，产物都必须能回到：

- 当前资料
- 当前笔记
- 当前问答上下文
- 当前知识库

否则这些能力就是悬浮模块。

---

## 6. 目标产品结构：改造后应该长成什么样

### 6.1 一级导航建议

一级导航建议收敛为四项：

1. **工作台**：承接搜索、提问、最近工作、快速导入
2. **资料**：承接资料浏览、搜索结果、来源阅读
3. **笔记**：承接个人沉淀、草稿、编辑、回访
4. **设置**：仅保留配置与系统信息

说明：

- 现在的 `Explore` 不应继续作为“工具收纳箱式”一级页存在
- `Spaces` 概念需要弱化成“知识库切换 / 当前库信息”，不必继续当作重页面
- `Settings` 需要从业务主流程中退出

### 6.2 首次启动结构建议

首次启动只允许出现三件事：

1. 打开已有 KB
2. 创建新 KB
3. 导入第一批资料

不要在首次启动时展示图谱、同步、模型配置、导出等二级能力。

### 6.3 工作台建议

工作台只做四件事：

1. 进入提问 / 搜索
2. 继续最近工作
3. 进入导入
4. 快速打开最近笔记 / 最近来源

工作台不应该成为“所有能力的集合页”。

### 6.4 资料页建议

资料页应该成为：

- 搜索入口
- 搜索结果列表
- 来源详情阅读
- 标签/时间/文件夹筛选

也就是说，它是“围绕外部资料工作”的地方。

### 6.5 笔记页建议

笔记页应该成为：

- 自己沉淀内容的主入口
- 草稿与正式笔记的统一工作区
- 从资料生成笔记、从问答生成笔记、从选区生成笔记的归一入口

当前 `reader / draft / notes` 混合模式，需要收敛成更明确的笔记工作模型。

### 6.6 高级工具建议

以下能力不再作为主导航页，而是改为上下文工具：

- Timeline
- Tags
- Entities
- Graph
- Report
- Podcast

例如：

- 在资料页中查看某文档的时间关联
- 在笔记页中查看当前笔记的关联标签或实体
- 在某次检索结果基础上生成报告
- 在当前主题上下文中生成播客脚本

---

## 7. 分阶段改造计划

## Phase 1：先收口产品主线，不碰太多深层能力

### 目标

把用户最先看到的导航、首页、设置、浏览器预期收敛清楚。

### Task 1: 明确产品对象与页面职责

- [ ] 梳理每个一级页面只负责一个主任务
- [ ] 删除或降级 legacy page 的一等可见性
- [ ] 输出页面职责矩阵（工作台 / 资料 / 笔记 / 设置）
- [ ] 明确哪些功能必须移出一级导航

### Task 2: 重构信息架构

- [ ] 让 `Explore` 从一级产品概念退场
- [ ] 让 `Spaces` 从重页面退化为 KB 切换机制或轻面板
- [ ] 把 `Settings` 只保留为配置页
- [ ] 为桌面-only 定位补齐更清晰的启动与错误文案

### Task 3: 定义最小用户任务闭环验收

- [ ] 写出首次上手闭环验收
- [ ] 写出日常问答闭环验收
- [ ] 写出阅读沉淀闭环验收
- [ ] 写出回访恢复闭环验收

---

## Phase 2：重建核心对象模型，让状态围绕工作对象组织

### 目标

把“系统状态很多”重构成“当前对象明确”。

### Task 4: 收敛前端状态模型

- [ ] 定义 `currentKb`
- [ ] 定义 `currentSource`
- [ ] 定义 `currentNote`
- [ ] 定义 `currentWorkspaceSession`
- [ ] 识别并合并重复 store 状态

### Task 5: 重构文档与笔记工作区

- [ ] 明确 reader / draft / notes 各自职责
- [ ] 决定是否合并为统一笔记工作区
- [ ] 让“保存为笔记”成为稳定的一等动作
- [ ] 让来源阅读到笔记沉淀形成最短跳转路径

---

## Phase 3：围绕主闭环重排功能入口

### 目标

让导入、搜索、问答、阅读、笔记之间的切换稳定且直观。

### Task 6: 重做首次启动流

- [ ] 打开 / 创建 KB 入口前置
- [ ] 首次导入成功后直接引导到搜索或问答
- [ ] 默认状态不要让用户在多个壳页间迷路

### Task 7: 重做工作台入口

- [ ] 只保留搜索 / 问答 / 最近工作 / 快速导入
- [ ] 去掉与主任务无关的噪音信息
- [ ] 保证工作台是“开始工作”，不是“看功能目录”

### Task 8: 重做资料页与来源页

- [ ] 搜索结果要稳定进入来源阅读
- [ ] 来源阅读要稳定进入摘录与笔记
- [ ] 标签/时间/文件夹能力围绕搜索与阅读出现

---

## Phase 4：把高级能力回流到主流程中

### 目标

保留高级功能，但取消其“抢首页”的资格。

### Task 9: 把高级页下沉为上下文工具

- [ ] Timeline 改为围绕资料或笔记的时序视图
- [ ] Tags 改为资料/笔记筛选与整理工具
- [ ] Entities 改为阅读/问答中的实体补充视图
- [ ] Graph 改为当前主题或当前对象的关系视图

### Task 10: 重新挂接报告与播客

- [ ] 报告必须从选定结果集或当前主题生成
- [ ] 播客脚本必须从当前资料上下文生成
- [ ] 产物必须能保存回 KB / 笔记系统

---

## Phase 5：收口设置与运维能力

### 目标

把“设置页像系统后台”的问题解决掉。

### Task 11: 拆分业务动作与系统配置

- [ ] KB 打开 / 创建从设置页前置出去
- [ ] AI 模型配置保留在设置页
- [ ] WebDAV / Obsidian / 导出放到高级配置区
- [ ] 通过渐进披露避免设置页成为大杂烩

### Task 12: 明确高级集成的功能等级

- [ ] WebDAV 是同步能力，不是主导航能力
- [ ] Obsidian 导入是迁移/导入能力，不是主任务能力
- [ ] 导出是收尾动作，不是频繁入口

---

## Phase 6：把验证从“零件正确”升级到“闭环成立”

### 目标

让后续每次迭代都围绕真实任务验收，而不是只验证组件渲染。

### Task 13: 建立闭环验收测试

- [ ] 首次启动闭环测试
- [ ] 导入后搜索闭环测试
- [ ] 搜索到阅读再到笔记闭环测试
- [ ] 回访恢复闭环测试

### Task 14: 建立桌面优先验收策略

- [ ] 浏览器预览继续只验证 runtime guard
- [ ] 桌面模式增加真实主路径验证
- [ ] UI 基线截图改为围绕主闭环维护

---

## 8. 里程碑定义

### Milestone 1：用户能明确理解产品是什么

满足条件：

- 一级导航收敛
- 浏览器与桌面预期清晰
- 首次启动只围绕 KB 与导入
- 设置页不再承载主业务流程

### Milestone 2：核心闭环可用

满足条件：

- 用户能打开 KB
- 用户能导入资料
- 用户能搜索 / 提问
- 用户能打开来源继续阅读
- 用户能把结果沉淀为笔记

### Milestone 3：高级能力不再打断主流程

满足条件：

- 时间线 / 标签 / 实体 / 图谱变成上下文工具
- 报告 / 播客依附主题上下文生成
- 高级配置退回设置区

### Milestone 4：产品进入稳定演进阶段

满足条件：

- 任务闭环测试覆盖主路径
- 状态模型围绕工作对象组织
- 新功能进入时必须声明自己属于哪条闭环、哪一层能力

---

## 9. 执行顺序建议

### 第一优先级（立刻开始）

- Product positioning 收口
- 信息架构重排
- 一级导航与首页重构
- 设置页降级

### 第二优先级

- 文档 / 笔记工作区重构
- 搜索、阅读、沉淀闭环打通
- 统一状态模型

### 第三优先级

- 高级能力回流
- 桌面端任务验收补齐
- 导出 / 同步 / 迁移分层整理

### 暂缓项

- 插件化扩展
- 更重的自动化体系
- 继续扩写三等功能页面
- 任何会继续分散主导航注意力的新增模块

---

## 10. 最终判断

### 10.1 现在的问题不是“缺功能”

ClawKB 现阶段已经有不少真实能力，问题不在于“不够强”，而在于：

- 功能等级没有划分
- 页面职责没有收口
- 主链路没有形成闭环
- 验证方式还没有升级到任务层

### 10.2 现在最重要的不是继续加模块

最重要的是先完成三件事：

1. **明确定位**：单用户、本地优先、围绕自己资料持续工作
2. **明确闭环**：KB → 导入 / 搜索 / 阅读 / 笔记 / 回访
3. **明确层级**：主功能一等可见，高级能力上下文化，系统配置退后

### 10.3 改造成功的标志

如果本计划执行成功，用户会明显感觉到：

- 我知道这个产品是干什么的
- 我知道我第一次应该怎么开始
- 我知道我日常应该从哪里进入
- 我能从资料快速走到自己的笔记
- 我不会再被一堆高级页面打断

这时 ClawKB 才会从“功能很多但混乱的原型”，变成“有稳定主轴的本地知识工作台”。

